{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AppSettings": {
    // URL base de los links de los emails de confirmación.
    // 10.0.2.2 solo existe dentro del emulador Android (alias del localhost del PC);
    // por eso este valor vive en el appsettings de DESARROLLO. En producción se
    // configura con la URL pública real (variable de entorno o appsettings.Production.json).
    "FrontendUrl": "http://10.0.2.2:5221"
  }
}
